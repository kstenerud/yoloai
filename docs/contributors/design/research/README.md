# Research

Research documents supporting yoloAI's design decisions. Each file covers a broad topic area with verified facts, community sentiment, and design implications.

| File | Topics |
|------|--------|
| [Reflink vs hardlink](reflink-vs-hardlink.md) | **DECIDED 2026-06-30 — both consumers use reflink-or-full-copy; hardlink rung dropped** (migrations are rare → full-copy fallback acceptable; avoids hardlink's replace-only footgun; unified with Phase-1 reflink-`:copy`). The two cheap "build-a-tree-alongside" primitives for `:copy` and migration snapshots. Tradeoff: **support breadth favors hardlink** (works on ext4/HFS+/native-NTFS/tmpfs where reflink doesn't — the "reflink is wider" intuition is likely backwards) vs **safe-by-construction favors reflink** (CoW protects the original from in-place edits + degrades to a correct full copy; hardlink's shared inode silently corrupts the old generation unless code is perfectly replace-only). Per-use-case fit differs: `:copy` (arbitrary agent edits in place) → reflink/copy only; migration snapshot (our own replace-only code) → hardlink viable & maybe preferable on ext4. Cross-platform support matrix **verified + cited** (hardlink ⊋ reflink confirmed; native NTFS/ext4/HFS+ have no reflink; Windows reflink needs an explicitly-created ReFS/Dev Drive); **macOS on-device results (verified 2026-06-30)**: APFS `clonefile` CoW-shares + is CoW-independent (the `:copy` property) and clones whole directory trees preserving perms/xattrs; cross-volume → `EXDEV`/silent full-copy fallback (staging must be same-volume); `F_FULLFSYNC` required for power-loss durability; reflink is the natural macOS default (hardlink buys nothing on APFS). **DF69 CONFIRMED**: macOS `:overlay` upper is tmpfs-only — uncommitted changes are lost on *any* restart (graceful or kill), on **all three** macOS backends (OrbStack, Docker Desktop, Apple `container`); the overlay→copy flatten must run while the sandbox is live. |
| [Migration-version gating](migration-version-gating.md) | **Verified 2026-06-30 (backs D109 overlay retirement; resolves its Open Q1).** The "stepping-stone upgrade" prior art for retiring an on-disk feature via a designated **migration version** + a newer version that **detects + refuses** leftovers: Elasticsearch (last-minor-of-prev-major + Upgrade Assistant; `IndexMetadataVerifier` five-element refusal = the gold standard), PostgreSQL (`pg_upgrade`, new binary literally can't read old state — the sharpest precedent), MongoDB FCV (raise-last + the stuck-transition anti-pattern), Kafka IBP/metadata.version (bump-last, downgrade-blocked), Kubernetes (one-minor skew + the best-in-class pre-upgrade warning header/metric), Debian (no-skip, weakest = policy-only). Takeaways: detect-and-refuse not auto-convert; name the exact migration version in a good refusal; ship a pre-upgrade audit verb; atomic stamp-flip + idempotent/resumable conversion; **delete the legacy *reader* from the new binary but keep the *detector* forever.** Pairs with [Crash-safe migration](crash-safe-migration.md). |
| [Crash-safe migration](crash-safe-migration.md) | **Verified 2026-06-30 (backs DF68 / [plans/crash-safe-migration.md](../plans/crash-safe-migration.md)).** Prior art for crash-safe `system migrate`: SQLite (rollback-journal/WAL, the delete-is-commit point, hot-journal detection), POSIX atomic-rename + fsync idiom (write-temp-same-dir → fsync → rename → fsync-dir; macOS `F_FULLFSYNC`), ARIES (WAL ordering + redo-idempotency + the CLR insight, with DPT/checkpoints/locking flagged as overkill), Nix/OSTree (build-alongside + atomic pointer-swap; retained generation *is* the snapshot; cheap CoW/hardlink retention), dpkg/rpm (**the cautionary tale** — both bet on roll-forward/resume; rpm shipped reverse-replay rollback and deleted it as "too unreliable"). Unanimous signal: **build-alongside + pointer-swap > mutate-in-place + restore-backup; forward=resume-first, rollback=wholesale snapshot-restore-only; journal=coarse per-sandbox state, not a fine WAL.** Maps to all six draft open questions. |
| [Competitors](competitors.md) | Existing tools, community pain points, feature comparison |
| [Agents](agents.md) | AI coding CLI agents, shell mode, Aider local-model integration |
| [Agent proxy support](agent-proxy-support.md) | **Verified mid-2026 (resolves Q37).** Whether each shipped agent honors `HTTP_PROXY`/`HTTPS_PROXY` (Claude/OpenCode yes; Gemini/Codex partial — Codex disables it *in its own sandbox*; Aider only with `DISABLE_AIOHTTP_TRANSPORT`). The design conclusion: env-proxy is **never** a containment boundary for an untrusted agent, and transparent intercept *alone* is bypassable (`NET_ADMIN`/UDP/IPv6) — the hostile-containment primitive is a **default-deny egress netns + forced proxy on a different principal/namespace**. Backs [netpolicy.md](../netpolicy.md) §Hostile. |
| [Agent self-update](agent-self-update.md) | **Verified 2026-06-26.** Whether the substrate must carry `AgentInstallMethod`. Claude's `installMethod`'s *only* load-bearing use is the auto-updater (docs + [#28625](https://github.com/anthropics/claude-code/issues/28625)); the PATH/`~/.local/bin/claude` warning we patch around is that machinery on a host-vs-image mismatch. `DISABLE_AUTOUPDATER=1` (settings.json `env`) makes `installMethod` **inert**. Decision: **delete `AgentInstallMethod`** from the substrate, disable Claude auto-update as static agent self-config, strip the stale `installMethod` from the seeded file — no manifest needed. Keep `hasCompletedOnboarding:true` seeded ([#4714](https://github.com/anthropics/claude-code/issues/4714)). Backs D97 / arch-principles §4. |
| [Agent global-context](agent-global-context.md) | **Verified mid-2026.** How each shipped agent (Claude/Aider/Gemini/Codex/OpenCode) ingests GLOBAL user-level context. 4 fit "append to one home-dir markdown file"; **Aider is the outlier** (no auto-read global file — needs file + a `--read`/`read:` declaration). Footguns (Gemini configurable name, Codex `AGENTS.override.md`+`CODEX_HOME`, OpenCode CLAUDE.md fallback), imports non-portable (Claude/Gemini only), injected block is the weakest layer, AGENTS.md convergence. Backs [agent-layer.md](../agent-layer.md) Context capability. |
| [Security](security.md) | Credential management, network isolation, proxy sidecar, Claude Code proxy support |
| [Egress-broker host reachability](egress-broker-host-reachability.md) | **Linux-verified 2026-06-28 (feeds D106 / egress-proxy 2b-2).** How an in-sandbox agent reaches the host-side key-injector, per backend, and where the injector binds so only the sandbox — not the LAN — reaches it. Three classes: bridge+NAT (docker/podman → bridge gateway IP), VM+CNI (containerd/apple → CNI gateway), host-process (seatbelt → `127.0.0.1`); tart unverified. `HostFromContainer` is **advisory-only** today (no `ExtraHosts` wired → `host.docker.internal` doesn't resolve on Linux Engine). Decision: **gateway-IP-for-both** via a backend `InjectorReach{BindHost,DialHost}` seam; the split is kept because **Docker Desktop** (LinuxKit VM) needs `host.docker.internal` dial + Mac-host bind. Isolation blocks the gateway unless allowlisted (deferred — Phase 1 is open-egress). Includes a runnable **Mac spike brief**. |
| [Secure credentials](secure-credentials.md) | **Verified 2026-06-25.** Best-practice secure credential delivery against the **duped-agent** (confused-deputy / lethal-trifecta) threat. Ecosystem survey (env-var-everywhere; no shipping agent brokers the key out; default-deny egress rare; isolation≠egress); verified injection→exfil incidents; prior-art patterns (short-TTL, broker-vends, ephemeral-mount-not-env, destination-scoped, unforgeable-mint à la IMDSv2); Anthropic WIF (`sk-ant-oat01`, 1h, verified); egress-proxy/token brokers; ssh-agent forwarding. Conclusion: the **egress-proxy/broker = netpolicy's reserved strategy** (one boundary point for creds+network) is the highest-impact *novel* control; layer with a hardened narrow allowlist, egress logging + canaries, and the copy/diff/apply review gate for the in-session residual. Ownership split: yoloai owns proxy/broker/egress + a short-lived-cred caller hook; the embedder mints/scopes/revokes. Feeds **DF38** + [netpolicy.md](../netpolicy.md). |
| [Principal isolation](principal-isolation.md) | **Multi-tenant (D59).** Current single-principal baseline + what breaks when a daemon serves many principals: physical partition + id unforgeability, workdir/aux path confinement (symlink/TOCTOU), enumeration, credential bundle; plus blind spots beyond the brief — container escape as cross-tenant boundary, runtime-daemon confused deputy, shared-resource poisoning, log secrets, handle revocation. Pre-design. |
| [Sandboxing](sandboxing.md) | Alternative filesystem isolation, macOS VM sandbox, macOS process/FS isolation |
| [Implementation](implementation.md) | Env var interpolation, Claude Code installation, Go libs vs shell commands, tmux defaults |
| [Parallel Agents](parallel-agents.md) | Field case study (2025-06): manual tmux parallelism, spec-driven development, **sandbox chaining / pipelines**. Defers idle-detection depth to Orchestration |
| [Orchestration](orchestration.md) | **External** signals + ecosystem: how each agent emits completion/idle (hooks/bell/SDK events), the orchestrator-tool landscape, git-worktree patterns, handoffs, user pain points |
| [Idle Detection](idle-detection.md) | **yoloAI's own** idle/done detection — audit of current code + pluggable detector architecture (the canonical internal-design doc) |
| [macOS Idle Detection](macos-idle-detection.md) | Platform deep-dive *supporting* idle-detection.md: macOS process-idle techniques (sysctl KERN_PROC, libproc, DTrace, Mach APIs, network monitoring) |
| [Agent callbacks](agent-callbacks.md) | **Verified 2026-06-25.** Native turn-finished/stop/idle callbacks across the four non-Claude shipped agents (gates the pluggable detection-**strategy** design). **Codex** `notify` → `agent-turn-complete` (rich JSON, `~/.codex/config.toml`); **Gemini** `AfterAgent` hook (GA v0.26.0+, `~/.gemini/settings.json` — **not** `Stop`, that name was a hallucination); **OpenCode** `session.idle` via plugin `event` hook or SSE `/event`; **Aider** `--notifications-command` fires on turn-complete but passes **zero payload** (bare `shell=True`). Verdict: heuristic is the *floor*, 3–4 agents get a native strategy; Aider's data-free pulse argues for a **thinner** strategy contract (binary "turn ended", not "hand me the message"). Resume flags noted per agent. Backs [idle-detection.md](idle-detection.md) + [agent-owned-detection plan](../plans/agent-owned-detection.md). |
| [Agentic Workflows](agentic-workflows.md) | Community sentiment on autonomous agents, TDD subagent patterns, authority splitting, review gap, skill ecosystem |
| [Podman](podman.md) | Podman backend: SDK options, Docker-compat socket, rootless file ownership, overlay concerns, implementation scope, open questions |
| [Linux VM Backends](linux-vm-backends.md) | VM-level isolation beyond Docker: Firecracker, gVisor, Kata Containers, Cloud Hypervisor, Lima, Sysbox; comparison matrix and yoloAI recommendation |
| [Apple `container`](apple-container.md) | **Spike-verified 2026-06-10.** Apple's `container` 1.0.0 (Linux OCI in per-container VMs, macOS 26/arm64) as a backend. `machine`≠`container` (whole-home mount is a `machine` default, not `container run`). Verified: live `:rw`/`:ro` virtiofs, in-guest overlayfs (CAP_SYS_ADMIN), in-guest iptables allowlist, clean `--format json`. Full env-var catalog; daemon via XPC (no socket env var). Conclusion: Docker-class caps + stronger isolation, reuses profile images. |
| [SSH Backend](ssh-backend.md) | **Deferred 2026-05.** Design for a remote SSH backend (connection model, provisioning, file sync, workdir modes, secrets, diff/apply). Value prop eroded by VS Code Remote Tunnels + `DOCKER_HOST=ssh://`. Interface-rename ideas lifted into layering W-L11. |
| [Runtime interface spike](runtime-interface-spike.md) | W11 spike — catalogs `Runtime` interface methods across 5 backends, classifies static-vs-dynamic, recommends BackendDescriptor + ≤14-method core + optional interfaces. |
| [Base image candidates](base-image-candidates.md) | **Parked 2026-05-24.** Survey of pre-built bases (devcontainers/universal, gitpod/workspace-full, buildpack-deps, …) as replacements for `debian:bookworm-slim` under `yoloai-base`. Gotchas: Docker-in-Docker, `yoloai` uid 1001, AI-agent layer doesn't compress. Tart/Seatbelt not applicable. Recommendation: defer; only honest win is `buildpack-deps:bookworm` and it's not worth the churn. |
| [Dind storage drivers](dind-storage-drivers.md) | Nested `dockerd` storage driver across Linux / Docker Desktop / OrbStack / Podman Machine. **Linux verified 2026-06-05**: overlay2 nests on a real-fs `/var/lib/docker` (O3) — fast + low-disk; fuse-overlayfs pin only papers over the overlay-on-overlay backing. Recommends overlay2 + real-fs volume, vfs fallback. macOS columns + O1 EINVAL root-cause **pending a Mac host** — includes a full repro recipe for a Mac agent. |
| [Shared-state concurrency](shared-state-concurrency.md) | **Verified 2026-06-15.** Daemon vs daemonless (Docker single-arbiter SPOF vs Podman's daemonless locks+SQLite — works but not free: lock-exhaustion, `database is locked`, the complexity moves not vanishes) + file-locking wisdom (`flock` not `fcntl`; atomic-rename for lock-free reads; the no-multi-file-atomic-commit limit; SQLite's NFS hard-limit; the envelope where file locks suffice). Verdict: yoloAI's file-locked multi-tool model is sound *inside the envelope* (single host, low contention, single-doc-per-domain), daemon stays optional. Backs [persistence-helper.md](../persistence-helper.md) / [D87](../../decisions/working-notes.md). |
| [Container init / keep-alive delineation](container-init-delineation.md) | **Verified 2026-06-14.** Where a substrate stops and the OS begins — the Tier 0–3 spectrum (isolation → PID-1 hygiene → supervision → orchestration), the PID-1 zombie/signal problem + thin-init fix (tini/dumb-init/`--init`), Tier-2 supervisors we must NOT build, and the Kubernetes gotcha museum (the `pause` neutral-init, init vs sidecar — KEP-753's 4-year saga, probes, shareProcessNamespace, restartPolicy/backoff). Backend audit: VM backends already keep the env up via the guest OS init; Seatbelt-P1 already uses `tail -f /dev/null`; only Docker/Podman weld the agent session to PID 1 (DF31). Recommends: substrate contract = "up + exec/transfer", keep-alive backend-native (`KeepAliveModel` property), agent launched via exec, monitor as a probe. Feeds [public-layering](../plans/public-layering.md), Q103, DF31. |
| [Tart `-xcode` base image](tart-xcode-base-image.md) | **A/B verified 2026-06-09; feature uncommitted, adoption undecided.** Cirrus `-xcode` images bake Xcode + default iOS runtime → "image mode" skips the host-Xcode mount + in-VM runtime download. Works end-to-end (iOS tests pass, no host mount), but base is ~87 GB vs ~32 GB. Documents the size tradeoff, the arm64 swift-syntax pin, and bugs found (promote-delete-running, sandbox-setup None-deref, parseVMList column drift, cache-key collision; one unresolved base-disappearance). |
