=== DF175 residual gap: files rm + files put of the same name ===
date    : 2026-08-04 16:29:14
backend : tart
sandbox : df175-rmput-tart
creating tart sandbox df175-rmput-tart (this takes a few minutes)
Warning: using OAuth credentials from ~/.claude/.credentials.json
  These tokens expire after ~30 minutes and may fail in long-running sessions.
  For reliable auth, run 'claude setup-token' and export CLAUDE_CODE_OAUTH_TOKEN instead.

Sandbox df175-rmput-tart created
  Agent:    claude
  Workdir:  /var/folders/h8/p75r4zq95d59q622q33pngzr0000gn/T/tmp.SWavN7HwOA (copy)

Run 'yoloai attach df175-rmput-tart' to start working (Ctrl-b d to detach)
host dir: /Users/karlstenerud/.yoloai/library/sandboxes/df175-rmput-tart/rw/files
guest dir: /Volumes/My Shared Files/rw/files
run id  : 162948 (probe names are suffixed with it; poisoned paths are single-use)

== P0  control: is this host still capable of serving stale guest reads at all?
        (provoked by writing into the share DIRECTLY — the CLI path is repaired now,
         so using it here would test the fix and report a broken harness)
  ok      guest reads v1 (caches the path)               AAAAAAAAAAAAAAAAAAAA
  ok      host side is correct                           ZZZ
  ok      guest reads STALE bytes (the mechanism)        AAA

== P1  control: does the DF175 fix still repair a REPLACEMENT? (put --overwrite over a read path)
  ok      guest reads v1 (caches the path)               AAAAAAAAAAAAAAAAAAAA
  ok      guest reads the NEW bytes (repaired)           ZZZ

== P2  THE GAP: files rm, then files put of the SAME name — no replacement, so no repair
  ok      guest reads v1 (caches the path)               AAAAAAAAAAAAAAAAAAAA
  ok      host side: the file is gone after rm           gone
  ?       guest immediately after rm                     
  ok      host side holds the new content                QQQQQ
  ?       put exit code                                  0
  ?       GUEST READS                                    AAAAAAAAAAAAAAAAAAAA
  ?       guest stat size                                20
  ==> SILENT CORRUPTION on tart: put exited 0 and the guest serves AAAAAAAAAAAAAAAAAAAA where the host
      holds QQQQQ. This is the original DF175 defect, unguarded.

== P3  control: a path the guest has NEVER read must arrive correctly
  ok      guest reads the NEW bytes                      QQQQQ

== P4  DF177: re-putting a DIRECTORY must replace it, and the guest must see the new tree
        (the replace unlinks the old directory, so on tart this IS an rm+put — it is safe
         only because the removal now drops the guest dentries. That coupling is the point.)
  ok      guest reads v1 of keep.txt                     AAAAAAAAAAAAAAAAAAAA
  ?       second directory put rc                        1
        yoloai: bundle-162948 was written to the exchange directory, but the sandbox still reads stale content for keep.txt and it could not be refreshed; the sandbox would act on content that never existed. Restart the sandbox (yoloai stop df175-rmput-tart && yoloai start df175-rmput-tart) to clear it
  ok      host: no nested copy (DF177)                   gone
  ok      host: source-removed file is gone              gone
  ?       GUEST READS keep.txt                           <empty>
  ?       guest still sees gone.txt?                     no

== TOTALS ==
   pass=12 fail=0 observations=7

P2 is the finding; P0/P1/P3 only license reading it.
Cleanup: yoloai destroy df175-rmput-tart --abandon-unapplied
