host: macOS 26.5.1 arm64
date: 2026-08-09 21:29:32 CEST | anchor=com.apple/yoloai_r

== R0 SETUP ==
        guest=192.168.64.11  host as seen from the guest=192.168.64.1
        streaming server up on 192.168.64.1:18643 (one line per second, 40s)
        allowed (the host) -> 200    denied (1.0.0.1) -> 000   (need non-000 then 000)
   PASS    R0: enforcement is live and the stream endpoint is the allowlisted destination

== R1 BASELINE — the stream runs to completion while the entry is present ==
        Without this, a stream that stops for its own reasons would read as a revocation working.
        bytes received at +5s: 54     at +11s: 108
   PASS    R1: the stream is genuinely live and growing while permitted

== R2 REVOKE MID-STREAM — the question ==
        before revocation: 45 bytes received, 3 pf state(s) mentioning 192.168.64.11
        revoking: pfctl -a com.apple/yoloai_r -t yb_dst_1 -T delete 192.168.64.1
        pfctl: No ALTQ support in kernel
        pfctl: ALTQ related functions disabled
        pfctl: 1/1 addresses deleted.
        allowlist now holds 0 address(es) — the revocation is applied
        8s after revocation: 126 bytes (was 45)

== R3 THE CONTROL — a NEW connection must now fail ==
        Without this, 'the stream kept running' cannot be told apart from 'the delete did nothing'.
        new connection to the same endpoint: 000   (must be 000)
   FAIL    R2/R3: THE STREAM SURVIVED REVOCATION. New connections are refused, but the connection that
             predated the delete kept receiving data. Removing an allowlist entry changes FUTURE
             policy only. So a reclaimed slot is not a stopped sandbox, and the exposure window is
             bounded by the lifetime of the longest open connection — which nothing bounds.

== R4 THE FIX — does killing states stop it, and what does that cost? ==
        pfctl -k kills state entries. If R2 showed survival, this is the missing step in every
        revocation path: reaping, liveness reinstall, and sandbox teardown alike.
        stream re-established: 45 bytes; states mentioning 192.168.64.11: 4
        pfctl -k 192.168.64.11 -> No ALTQ support in kernel ALTQ related functions disabled 
        states now: 0
        6s after the kill: 108 bytes (was 45)
   FAIL    R4: the stream survived even a state kill (45 -> 108)
        
        and what it costs at the security boundary — the shipped D132 grant permits four forms:
          -a <anchor> -t <table> -T add|delete|flush|show ...   /   -a <anchor> -f <pinned file>
          -a <anchor> -s rules                                  /   -s info
        `-k` is NONE of them, and it is not scoped to an anchor: 'pfctl -k <host>' reaches every
        state on the machine, including the user's own. Granting it is a materially wider permission
        than table membership, so if R2 showed survival, the fix is a D132 amendment and not a patch.

== R5 WHY DID IT SURVIVE THE KILL? — diagnosing, not asserting ==
        R4 killed every state naming the guest and the transfer still advanced. That should not happen
        if the block rule is reached, so something is re-permitting the flow. The leading hypothesis:
        the pool's rules are all `in`. The HOST's outbound data is `out` on the bridge, matches no
        rule, meets pf's default pass — and a passed packet CREATES STATE. That new state then carries
        the rest of the connection, including the guest's replies, straight past the inbound block.
        
        If that is right, a rule covering the return direction closes it. Tested, with the control that
        legitimate allowed traffic must still work — a fix that blocks everything is not a fix.
        
        TWO ARMS, because the first run of this section conflated them. It loaded the rule AND ran
        `pfctl -k`, saw the transfer stop, and concluded "the fix is a rule rather than a grant".
        The kill was still in the sequence, so that arm proves the rule NECESSARY and never SUFFICIENT.
        R5a is the rule alone. R5b restores the kill. Only both together complete the 2x2:
             no-kill / no-rule -> SURVIVED (R2/R3)        kill / no-rule -> SURVIVED (R4)
             no-kill / rule    -> R5a, below              kill + rule    -> R5b, below
        CONTROL with the return-direction rule loaded: allowed destination -> 200
           (must still be non-000, or the rule is over-blocking and cannot be used)
        
        R5a  RULE ONLY — revoke the allowlist entry and do NOT run `pfctl -k`. This is the arm
             the shipped prescription rests on, and the one that had never been run.
        stream running: 54 bytes; states naming the guest: 4
        states, verbatim, before the revocation:
          ALL tcp 192.168.64.1:18643 -> 192.168.64.11:46468       TIME_WAIT:TIME_WAIT
          ALL tcp 192.168.64.1:18643 -> 192.168.64.11:43614       ESTABLISHED:ESTABLISHED
          ALL tcp 192.168.64.1:18643 <- 192.168.64.11:33916       TIME_WAIT:TIME_WAIT
          ALL tcp 192.168.64.1:18643 <- 192.168.64.11:39570       ESTABLISHED:ESTABLISHED
        after revoke WITHOUT the kill, states naming the guest: 4
          ALL tcp 192.168.64.1:18643 -> 192.168.64.11:46468       TIME_WAIT:TIME_WAIT
          ALL tcp 192.168.64.1:18643 -> 192.168.64.11:43614       ESTABLISHED:ESTABLISHED
          ALL tcp 192.168.64.1:18643 <- 192.168.64.11:33916       TIME_WAIT:TIME_WAIT
          ALL tcp 192.168.64.1:18643 <- 192.168.64.11:39570       ESTABLISHED:ESTABLISHED
        6s later: 108 bytes (was 54)
   FAIL    R5a: the transfer SURVIVED the rule alone (54 -> 108)
             pf does not re-evaluate rules for packets matching an existing state, so a rule
             loaded while that state exists cannot reach this connection — the same mechanism
             that made the in-only rules fail in R2/R3.
        
        R5b  RULE + KILL — the arm the earlier run actually ran, reproduced for comparison.
        stream running: 45 bytes; states naming the guest: 5
        after revoke + kill, states naming the guest: 2
          ALL tcp 192.168.64.1:18643 -> 192.168.64.11:46468       TIME_WAIT:TIME_WAIT
          ALL tcp 192.168.64.1:18643 -> 192.168.64.11:43614       TIME_WAIT:TIME_WAIT
        6s later: 45 bytes (was 45)
   PASS    R5b: rule + kill stops the transfer
        
        R5 VERDICT — read off the two arms above, not asserted:
             rule alone: survives        rule + kill: stops
   FAIL    R5: the rule is NECESSARY BUT NOT SUFFICIENT — only rule+kill stops an established flow.
            Every prescription that drops `pfctl -k` in favour of the rule is therefore WRONG.
            The remedy costs a D132 amendment, and `-k` is not anchor-scoped: 'pfctl -k <host>'
            reaches every state on the machine, including the user's own. Two alternatives are
            untested here: `pfctl -K` (kills by both endpoints, so a narrower grant), and a short
            tcp.established timeout inside the anchor, which bounds the window with no new grant.

== WHAT WAS NOT TRIED ==
        - UDP, and long-lived idle connections. Only an actively streaming TCP connection was used;
          pf's state timeouts differ per protocol and per state, and an idle TCP state can persist
          for hours by default.
        - `pfctl -K` (kill by both endpoints), which would be narrower than `-k` and might be a
          more defensible grant. Not tried, and it is the obvious next question if -k is too wide.
        - Whether reloading the pool ruleset (the liveness repair path) drops states by itself.
          That would make repair disruptive to innocent sandboxes, and it is untested.
        - State timeouts as an alternative to killing: a short `tcp.established` timeout in the
          anchor would bound the survival window without any new grant. Not measured.

== cleanup ==
   anchor rules: 0  main-refs: 2

pass=3 fail=4 unknown=0
results: /Users/karlstenerud/Projects/yoloai/docs/contributors/design/research/macos-isolation-spike/results/pf-revocation.txt
