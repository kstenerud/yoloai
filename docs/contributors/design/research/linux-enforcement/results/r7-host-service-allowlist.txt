host: Linux 6.8.0-136-generic x86_64 | docker 29.6.1 | nft v1.0.9
date: 2026-08-11 12:25:10 UTC
harness: research_harness_v1 (D134)
answers: the enforcement plan's inference that a brokered agent survives a sever (Q1), and
         the host-service allowlist the forward hook cannot express (Q2, R6's requirement)

=== R7 ===
Does a forward-hook deny-all leave the host gateway reachable, and can an input-hook allowlist permit only the broker?

-- controls (all held, or this would not render) --
  sandbox reaches the broker port with no policy: True
  sandbox reaches the other host service with no policy: True
  sandbox reaches an external host with no policy: True
  ARM B: the forward deny rule actually fired: True   (drop counter 4 — a zero counter with a blocked probe would be a free negative)
  ARM C: the input deny rule actually fired: True   (drop counter 4)
  ARM C: the broker accept rule actually matched: True   (accept counter 6 — proves the reach was decided by our rule and not by the absence of one)

-- measurements --
  ARM B: reaches the broker under forward deny-all: True
  ARM B: reaches an external host under forward deny-all: False
  ARM C: reaches the broker under the host-service allowlist: True
  ARM C: reaches the other host service under the allowlist: False

-- expectations, rendered from the values above --
  [PASS] severing the forward hook does NOT cut the path to the host gateway  <- ARM B: reaches the broker under forward deny-all=True
  [PASS] severing the forward hook does cut external egress  <- ARM B: reaches an external host under forward deny-all=False
  [PASS] an input-hook allowlist keeps the broker reachable  <- ARM C: reaches the broker under the host-service allowlist=True
  [PASS] an input-hook allowlist denies every other host service  <- ARM C: reaches the other host service under the allowlist=False

-- WHAT WAS NOT TRIED --
  - UDP, including DNS. Docker's embedded resolver lives at 127.0.0.11 inside the sandbox's own netns, so it is not host-destined and this run says nothing about a sandbox configured to use the host as its resolver
  - the real credential broker. Both host services here are `python3 -m http.server`; the injector's actual bind address and reachability (runtime.InjectorReach, DialHost) are not exercised
  - rootless podman and containerd. docker only — rootless podman's netns changes the whole question and containerd shares one bridge
  - macOS, where the S3 shape blocks in BOTH directions on the bridge and the gateway is therefore inside the enforced surface. This result must NOT be read across
  - IPv6
  - whether the host's REPLY path is affected: replies leave via output/postrouting, not forward, and that was assumed rather than measured
  - a hostile guest. Every probe here is a cooperative curl; nothing tests an agent trying to defeat the input-hook rules
  - concurrency, and any second sandbox on the same bridge

-- all expectations held --
