host: Linux 6.8.0-136-generic x86_64 | nft v1.0.9
date: 2026-08-12 02:33:30 UTC
harness: research_harness_v2 (D136)
round:   enforcement-verification-round-2.md, item V1

=== V1 ===
Does the nftables netlink group report every way our chain stops enforcing, attributably?

-- controls and baselines (all held, or this would not render) --
  baseline: an event naming our table appears in the monitor: False   (nothing has touched our table yet, so silence here is the floor)
  the chain is actually enforcing: True   (ground truth; an event about a chain that does not enforce is noise)
  enforcement really did stop: True
  re-armed for fault 2: True
  enforcement really did stop after the flush: True
  re-armed for fault 3: True
  enforcement really did stop after the reload: True
  re-armed for fault 4: True
  the table still exists right before the device is removed: True

-- measurements --
  after loading our own chain: True
  FAULT 'delete our table': an event arrived: True
    ... and it names our table: True
  FAULT 'flush ruleset': an event arrived: True
    ... and it names our table: True   (if not, the signal is 'something happened' and the detector must re-check every sandbox rather than one)
  the host-wide flush also destroyed docker's own chains: True   (L3d's finding from the other direction: a manager's flush takes every table with it, ours and docker's alike. Part 3's reconcile runs on a host where the runtime itself may be broken)
  FAULT 'reload with flush ruleset': an event arrived: True
    ... and it names our table: True   (the systemctl-restart-nftables shape the prior art says emits no firewalld signal — but this is nft's own group, not firewalld's)
  FAULT 'device deleted': the monitor was SILENT: True   (the fault V5 predicts is unreportable: the chain binds by ifindex, the device is gone, and no ruleset object changed — so there is nothing to send an event about)
    ... the table is still listed as present: True   (which is exactly why an inventory check cannot see this either)

-- expectations, rendered from the values above --
  [PASS] the monitor reports our table being created, so it is alive and attributable  <- after loading our own chain=True
  [PASS] at least one way of losing enforcement emits no event at all, so a subscription cannot be the whole detector  <- FAULT 'device deleted': the monitor was SILENT=True

-- UNBASELINED CLAIMS, resting on a probe never shown to report failure --
  at least one way of losing enforcement emits no event at all, so a subscription cannot be the whole detector: a silence, measured against a watcher this run already showed reporting our table; there is no mechanism to remove for a before/after on the absence itself

-- WHAT WAS NOT TRIED --
  - a Go netlink subscriber. `nft monitor` is the userspace face of the same group, which keeps this about the kernel — but what NFNLGRP_NFTABLES delivers to a raw socket may differ in detail, and the implementation must re-check
  - iptables-nft traffic from docker. Docker writes iptables rules that land in nft tables on this host, so its churn may be a substantial event source; nothing here measures the noise floor a real subscriber would face
  - firewalld's D-Bus reload signal, which is the prior art's actual mechanism and a different bus entirely. firewalld is not installed on this host
  - event LATENCY and loss. Each arm sleeps and then reads; a subscriber under buffer pressure can lose events, and NFNLGRP overruns are a documented class
  - whether an event can be attributed to a SANDBOX rather than a table, which is what the response policy actually needs
  - concurrency: two sandboxes' tables changing at once
  - the device REAPPEARING, which is R11 and V5's ground

-- all live expectations held --
