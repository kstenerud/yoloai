host: Linux 6.8.0-136-generic x86_64 | docker 29.6.1 | nft v1.0.9
date: 2026-08-11 12:44:28 UTC
harness: research_harness_v1 (D134)
answers: whether the netdev mechanism R10/R11 favour can express the policy this design
         actually needs -- a set-based allowlist, not the single-address drop it was
         recommended on
note: run 1 reported 'reject is NOT available in a netdev chain'. That was a FREE
      NEGATIVE: the probe was a one-line table definition nft cannot parse, so it
      failed on a closing brace and never reached the verdict keyword. Caught by
      running the identical rule with 'drop', which failed identically. That control
      is now inside the script, and the answer reversed. Run 1 went to stdout and was
      never written to a file, so this note is the only artifact of it.

=== R12 ===
Can a netdev chain hold a set-based allowlist, revoke live, and keep the gateway carve-out?

-- controls (all held, or this would not render) --
  allowlisted host reachable with no policy: True
  denied host reachable with no policy: True
  DNS resolves with no policy: True
  the identical rule with `drop` loads: True   (without this, a `reject` failure is a statement about my syntax)
  the set loaded with its element: True
  the deny rule fired: True   (5 packets)
  the allow rule matched: True   (6 packets — proves the reach was decided by our rule)
  the deny rule fired again after revocation: True   (5 -> 10)
  the reject-shaped policy still blocks: True   (took 0.07s)
  the reject rule fired: True   (1 packets)

-- measurements --
  `reject` loads in a netdev chain: True
  allowlisted host reachable under the set policy: True
  denied host blocked under the set policy: False
  DNS still resolves under the set policy: True
  the formerly-allowed host is blocked after a set delete: False
  re-adding the element restores reach: True
  reject fails FAST in a netdev chain, as it does in the forward hook: True   (reject 0.07s vs drop 5.06s. If these are equal, the keyword loads and the sender still sees a black hole — netdev ingress has no routing context to build a response from)
  drop blocked too, so the comparison is like-for-like: True   (drop took 5.06s)

-- expectations, rendered from the values above --
  [PASS] a netdev chain accepts reject  <- `reject` loads in a netdev chain=True
  [PASS] a named set works as an allowlist in a netdev chain  <- allowlisted host reachable under the set policy=True
  [PASS] and everything outside it is denied  <- denied host blocked under the set policy=False
  [PASS] without collaterally killing DNS  <- DNS still resolves under the set policy=True
  [PASS] revocation is a live set update, with no chain reload  <- the formerly-allowed host is blocked after a set delete=False
  [PASS] and it is reversible  <- re-adding the element restores reach=True
  [PASS] a netdev reject gives the sandbox an immediate error, not a hang  <- reject fails FAST in a netdev chain, as it does in the forward hook=True

-- WHAT WAS NOT TRIED --
  - allowlist SIZE. One element. p2 measured the forward hook at 1/1000/10000 and found run-to-run variance larger than any difference between configurations; nothing here prices a netdev chain at any size, and the hook is per-packet on every device
  - an in-flight transfer. Revocation here is measured on a NEW connection; p1b's sustained-transfer shape has not been run against a netdev chain, and that is the arm where the conntrack fast-path mattered
  - IPv6 and ARP, which this policy deliberately lets past the chain policy rather than judging. A v6-capable host would need the v6 half written and measured
  - the gateway carve-out against the REAL broker. `10.207.0.1` is allowlisted by address here; r7 measured the broker path on the forward/input hooks, not this one
  - the reply direction and `hook egress`
  - whether a set can be shared across sandboxes' chains, which is what the reaping design's per-sandbox records would want
  - rootless podman and containerd/CNI. docker only
  - a hostile guest attempting to influence the chain or its set

-- all expectations held --
