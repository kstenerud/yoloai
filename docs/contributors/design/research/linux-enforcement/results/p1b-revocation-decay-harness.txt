host: Linux 6.8.0-136-generic x86_64 | docker 29.6.1 | nft v1.0.9
date: 2026-08-11 07:21:00 UTC
harness: research_harness_v1 (D134) — port of p1b_revocation_decay.sh
compare: results/p1b-revocation-decay.txt (the bash original)

=== P1b ===
Does removing an allowlist entry stop an in-flight transfer, and does the conntrack fast-path prevent that?

-- controls (all held, or this would not render) --
  with the fast-path, the revoked transfer keeps flowing: True   (KB/s across the window: [300, 300, 300, 300, 300, 300])
  with the fast-path, no packet ever reaches the deny rule: True   (drop counter moved by 0)

-- measurements --
  without the fast-path, the transfer stops: True   (KB/s across the window: [121, 0, 0, 0, 0, 0])
  without the fast-path, the deny rule actually fired: True   (drop counter moved by 11)

-- expectations, rendered from the values above --
  [PASS] revocation stops an in-flight transfer once the fast-path is gone  <- without the fast-path, the transfer stops=True
  [PASS] the stop is caused by the deny rule, not by the transfer ending  <- without the fast-path, the deny rule actually fired=True

-- WHAT WAS NOT TRIED --
  - whether the client surfaces an error or hangs — curl is given -m 120, and what a real agent's HTTP stack does with a stalled socket is untested
  - UDP
  - the reply direction: bulk data flows server->client, matches no rule, and falls through an accept policy. macOS measured this direction to be load-bearing there (pf-no-state.txt); nothing here measures it on Linux
  - interface keying. These rules say `ip saddr` because the original did, so the two runs are comparable. The fast-path result is independent of the key, but that is an argument, not a measurement
  - a destination with more than one address — P1 run 1 was invalidated by exactly that, and this uses a local nginx to avoid it rather than to settle it
  - concurrency: one client, one flow, one server

-- all expectations held --
